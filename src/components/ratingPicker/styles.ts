import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    picker: {
        marginVertical: 0,
        width: '100%',
        height: 200,
        padding: 8,
        borderColor: "#666",
      },
});

export default styles;